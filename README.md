# 🃏 Белот Telegram Бот — Инструкция по установке

## Структура файлов

```
belot_bot/
├── bot.py          — главный файл, запуск бота
├── game.py         — логика игры Белот
├── game_manager.py — менеджер активных игр
├── cards.py        — карты, колода, очки
├── declarations.py — комбинации (терц, каре, белот)
├── handlers.py     — Telegram обработчики
├── keyboards.py    — кнопки интерфейса
├── requirements.txt
├── Dockerfile
└── README.md
```

---

## Шаг 1 — Создать Telegram бота

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Введите имя: например `Белот Молдова`
4. Введите username: например `belot_moldova_bot`
5. Скопируйте полученный **TOKEN**

---

## Шаг 2 — Где хостить (рекомендации)

### Вариант A: Railway (бесплатно, просто) ⭐ РЕКОМЕНДУЕТСЯ

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. New Project → Deploy from GitHub repo
4. Загрузите файлы в GitHub репозиторий
5. В Railway: Settings → Variables → добавьте:
   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   ```
6. Deploy → бот запускается автоматически!

### Вариант B: Render (бесплатно)

1. Зайдите на [render.com](https://render.com)
2. New → Web Service → Connect GitHub
3. Build Command: `pip install -r requirements.txt`
4. Start Command: `python bot.py`
5. Environment Variables: `BOT_TOKEN=ваш_токен`

### Вариант C: VPS (DigitalOcean, Hetzner, etc.)

```bash
# На сервере:
git clone ваш_репозиторий
cd belot_bot
pip install -r requirements.txt
BOT_TOKEN="ваш_токен" python bot.py

# Для постоянной работы (systemd):
sudo nano /etc/systemd/system/belot.service
```

```ini
[Unit]
Description=Belot Telegram Bot
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/belot_bot
Environment="BOT_TOKEN=ваш_токен"
ExecStart=/usr/bin/python3 bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable belot
sudo systemctl start belot
```

### Вариант D: Docker

```bash
docker build -t belot-bot .
docker run -d -e BOT_TOKEN="ваш_токен" --name belot belot-bot
```

---

## Шаг 3 — Запуск локально (для теста)

```bash
cd belot_bot
pip install -r requirements.txt
export BOT_TOKEN="ваш_токен"
python bot.py
```

---

## Как играть

1. Найдите бота в Telegram
2. Напишите `/newgame` — создать игру
3. Отправьте ссылку 3 друзьям (или код игры)
4. Когда все 4 игрока зашли — игра начнётся автоматически
5. Карты приходят в личку каждому игроку
6. Нажимайте кнопки для торгов и хода картами

---

## Правила (Moldova / belot.md)

- 4 игрока, 2 команды (0+2 vs 1+3)
- 32 карты (7–Туз)
- Козырной Валет = 20 очков, Девятка = 14 очков
- Комбинации: Терц(20), 50, 100, Каре(100/150/200)
- Белот (K+Q козыря) = 20 очков
- 8888 = аннулирует все комбинации
- 7777 = аннулирует раунд
- Если перевёрнут Валет = автоматический козырь
- **Победитель — кто первый наберёт 151 очко**

---

## Команды бота

- `/start` — главное меню
- `/newgame` — создать игру
- `/join КОД` — войти в игру
- `/help` — правила
